//논리 연산자
// 여러개의 참/거짓 조건을 조합ㅂ
//결과: boolean

//and : &&, or:||


let age = 20;
let hasId = false;

// 주류 구매 조건
console.log(age>=20 && hasId); //false
console.log(age>=20 || hasId); //true
console.log(!hasId); // not 연산자 -> 값 반대로 
